import { json, handleOptions } from "../_shared/cors.ts";
import { env, supabaseConfig } from "../_shared/env.ts";
function authorised(req:Request){const s=env("WORKER_SECRET")??"";return !!s && req.headers.get("x-worker-secret")===s}
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return handleOptions();if(!authorised(req))return json({error:"Unauthorised"},401);const{url,serviceRoleKey}=supabaseConfig();const root=url.replace(/\/+$/g,"");const h={apikey:serviceRoleKey,Authorization:`Bearer ${serviceRoleKey}`,"Content-Type":"application/json"};const tr=await fetch(`${root}/rest/v1/tenants?select=id,retention_days`,{headers:h});if(!tr.ok)return json({error:"Failed to load tenants"},502);const tenants=await tr.json() as Array<Record<string,unknown>>;let deleted=0;for(const t of tenants){const tid=String(t.id),days=Math.max(1,Number(t.retention_days??365));const cutoff=new Date(Date.now()-days*86400000).toISOString();const br=await fetch(`${root}/rest/v1/chatbots?tenant_id=eq.${tid}&select=id`,{headers:h});const bots=await br.json() as Array<Record<string,unknown>>;for(const b of bots){const r=await fetch(`${root}/rest/v1/conversations?chatbot_id=eq.${encodeURIComponent(String(b.id))}&updated_at=lt.${encodeURIComponent(cutoff)}`,{method:"DELETE",headers:{...h,Prefer:"return=representation"}});if(r.ok){const rows=await r.json() as unknown[];deleted+=rows.length}}}await fetch(`${root}/rest/v1/idempotency_keys?expires_at=lt.${encodeURIComponent(new Date().toISOString())}`,{method:"DELETE",headers:{...h,Prefer:"return=minimal"}});return json({ok:true,deletedConversations:deleted});});
