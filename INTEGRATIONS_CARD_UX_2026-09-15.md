# Integrations card UX update — 2026-09-15

- Replaced raw internal capability strings on connection cards with customer-facing action chips.
- Added provider-specific descriptions focused on what each integration lets the assistant do.
- Limits visible action chips to four and shows a compact +N more indicator.
- Keeps internal capability keys unchanged for routing/security.
- Provider icons remain keyed by immutable provider ID with provider-name fallback; custom providers retain neutral fallback.
- Connection status and Test / Configure / Disconnect behaviour are unchanged.
